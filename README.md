## Building Spin component with javascript

This is an example of building spin apps with jco

## building and running

First build the SDK

```
cd spin-sdk
npm install
npm run build
```

Build and run the app from the root of the project

```
npm install
spin build --up
```